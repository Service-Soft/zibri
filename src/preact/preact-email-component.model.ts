import { PreactComponent } from './preact-component.model';

/**
 * Definition of a valid preact email component that can be rendered by zibri.
 */
export type PreactEmailComponent<T = {}> = PreactComponent<T>;